{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Bat-O-Meter",
      "type": "debugpy",
      "request": "launch",
      "module": "batometer.main",
      "cwd": "${workspaceFolder}/src",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
