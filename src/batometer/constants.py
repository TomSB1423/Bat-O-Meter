BATOMETER = "Batometer"
VIDEO_FPS = None  # Will be set dynamically at runtime based on the loaded video

# Key codes for navigation and toggles
LEFT_KEYS = [81, 2]  # 81: arrow left, 2: macOS/Linux
RIGHT_KEYS = [83, 3]  # 83: arrow right, 3: macOS/Linux
ESC_KEYS = [27]
SPACE_KEYS = [ord(" ")]
TRACK_KEYS = [ord("t"), ord("T")]
AVERAGE_HEATMAP_KEYS = [ord("h"), ord("H")]
